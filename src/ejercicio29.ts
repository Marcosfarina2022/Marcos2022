let numeros: number[] = new Array(5);
for (let indice = 0; indice < 5; indice++) {
  numeros[indice] = Number(prompt("Ingrese los valores"));
}
for (let indice = 0; indice < 5; indice++) {
  console.log(numeros[indice]);
}
